# Program Closeout Ledger — buckholtz-idm-multing-mvp

**Baseline: 2026-09-13.** Built ONLY from `CURRENT_EVIDENCE_STATE.md`
(no new derivation, computation, or claim introduced here) — a frozen
snapshot marking the end of this project's discovery campaign and the
start of consolidation → publication → external challenge. Not "truth
forever" — a dated baseline, reopened only per each claim's own
`Reopen if` line, not by default drift.

**Six fields per claim:** `Claim → Evidence IDs → Status → Strongest
support → Strongest counterevidence/limitation → Reopen only if`.

**Status vocabulary (fixed 2026-09-13, not interchangeable):**
`SOLVED` (specific, scoped question fully answered) · `CLOSED/BOUNDED`
(answered within a stated operationalization — never read as broader
validation) · `DISSOLVED` (the question had no content) · `EXHAUSTED`
(a bounded search campaign ran out; underlying physical question may
stay open) · `STOPPED/UNRESOLVED` (an active decision not to continue,
not a proof of nothing-to-find) · `NON-IDENTIFIED` (not answerable
from what exists, structurally, not a temporary gap).

`NO_AUTHOR_ERROR` throughout: no row below is a claim about MULTING's
own physical correctness — every verdict is about this project's own
reconstruction, unless a row explicitly says otherwise.

---

## Bottleneck-map claims (`docs/147`, `CURRENT_EVIDENCE_STATE.md` §4)

### 1. F→H_MULT(z) causal-compatibility (bottleneck 1)
- **Evidence IDs:** `FINDING_P223` (commit `e421126`), `docs/153`, `docs/156`, `docs/127`.
- **Status:** CLOSED/BOUNDED — within the literal-text reading only.
- **Support:** `P223` embeds v82's own force law into v82's own kinematic-translation machinery; symbolically confirmed `k_A(z)`-dependent terms survive (H1); independently reproduces this project's own `docs/127` `G_αβ=0` for the same power-law shapes (H2). Synthesis (H3): the two closures answer structurally different questions about a shared force-law shape — not in contradiction.
- **Limitation:** Does NOT establish MULTING is physically correct. v82's own physical INTENT for `s(z)` carrying unstated population content is explicitly out of scope (Skeptic Objection 2, accepted).
- **Reopen only if:** v82's own intent for `s(z)` is clarified with new population content, or a new publication changes the construction.

### 2. Unique completion (bottleneck 2)
- **Evidence IDs:** `docs/134`, `P75`, `P78`, `P79`, 2026-09-07 `/hypothesis-arbiter` run.
- **Status:** STOPPED/UNRESOLVED.
- **Support:** `P75` proved completion-blindness of the structural layer only (narrow). `/hypothesis-arbiter` on the dipole's ontology (`H_body`/`H_fluid`/`H_avg`) returned only the least falsifiable survivor.
- **Counterevidence/limitation:** `P78`'s apparent dynamical discrimination was retracted by `P79`'s own robustness gates. The `P132`-`P139` round never attacked this bottleneck directly. The arbiter's stop recommendation reflects an EVI judgment, not a proof the question is unanswerable.
- **Reopen only if:** a genuinely new input — not another completion-candidate variant.

### 3a. Absolute scale — `(A,g,κ)` (bottleneck 3, first object)
- **Evidence IDs:** `FINDING_P206`, `FINDING_P52`, `P133`.
- **Status:** DISSOLVED.
- **Support:** `P133`'s rank-2 result IS the field-normalization redundancy `P52` already derived 8 days earlier; `L := 2a−b−c` is the field-rescaling weight, the null direction is the `φ→λφ̄` generator. Rank 2 is complete: 3 coordinates, 1 redundancy, 2 physical d.o.f. Step 8a run twice, both CONFIRMED-REAL.
- **Limitation:** `P52`'s own KG2 residue (`Ag²`, `η`) remains structurally unmeasurable — see row 3c below.
- **Reopen only if:** nothing — the question as posed had no solution and needed none.

### 3b. Absolute scale — v82's own `(β1,β2)` Fisher-forecast line (bottleneck 3, second, separate object)
- **Evidence IDs:** `P190`-`P194`.
- **Status:** EXHAUSTED (search campaign only — physical question stays open).
- **Support:** `P191`: synthetic `H(z)` at `z∈{3,5,7,10}` shrinks the degeneracy ellipse 73/46/14% at 1/3/10% precision (MCID met). `P192`: real fitted `(β1,β2)` make `H²(z)` undefined past `z≈16.957` (grid-converged boundary). `P193`: `z∈{12,14,16}` gives up to 5.4× more shrinkage. `P194`: full window scan confirms monotonic rise, entangled among ≥3 co-varying mechanisms (not a clean single cause).
- **Counterevidence/limitation:** No real high-z `H(z)` data exists at any assumed precision — this is an in-principle path, not an executed measurement. No next candidate named since 2026-09-05.
- **Reopen only if:** real high-z data materializes, or a genuinely new (not z-set-variant) question is posed.

### 3c. `P52`'s residual — `Ag²` and `η` (KG2, tied to row 3a)
- **Evidence IDs:** `P208`, `P209`, `P23`, `P149`, `P53`, `P24`, `P25`, MICROSCOPE `arXiv:2209.15487`.
- **Status:** NON-IDENTIFIED, both invariants, each for a different structural reason.
- **Support:** `Ag²` is degenerate with `G` (absorbed into the measured Newton constant, `P23`/`P149`). `η` enters observables only as a product with an unknown composition contrast (`η·|Δψ| ≤ 2.5×10⁻⁸ m`, ~2σ, from real MICROSCOPE data) — the first external number ever to bear on `η`, still not `η` itself.
- **Limitation:** blocked MODEL-side, not data-side — the best flown equivalence-principle experiment is sharp enough to bite; the model never made the needed postulate definite.
- **Reopen only if:** the model defines `Δψ` (the `k_A,k_P` row `MODEL_SPEC_AUDIT.md` flags OPEN).

### 4. IC-sensitivity (bottleneck 4)
- **Evidence IDs:** `P135`, `P136`, `P138`, `P139`, `P141`, `docs/148`.
- **Status:** SEARCH CAMPAIGN EXHAUSTED (physical question not necessarily resolved).
- **Support:** 5 mechanism candidates excluded (pole REFUTED, early-transient REFUTED, horizon-crossing DESIGN-LIMITED, gauge WEAKENED-BY-REASONING, mode-projection REFUTED via structured inverse-problem synthesis, not a guess).
- **Counterevidence/limitation:** synthesis found no new candidate — explicit user-set stop, not a mechanism-level answer. The growth-channel discriminator's own IC-sensitivity at low `k` remains genuinely open as physics.
- **Reopen only if:** a genuinely new mechanism class (not equivalent to the 5 excluded), a new verified external fact, or explicit user override for a 6th attempt.

---

## Core physics claims

### 5. F_oP bilinear structure / alternating-sign theorem
- **Evidence IDs:** `docs/125`, `P1`.
- **Status:** SOLVED (as a mathematical derivation, not a physical validation).
- **Support:** exact 2-species form, alternating-sign rule derived as a theorem for the two-charge completion, not postulated.
- **Limitation:** a derivation about this project's own reconstruction; not evidence MULTING itself is correct.
- **Reopen only if:** a structural error is found in the derivation.

### 6. Isotropic-average k-sector = 0 (double-layer theorem)
- **Evidence IDs:** `docs/127`, S-S background-coupling result.
- **Status:** SOLVED.
- **Support:** exactly zero at force level (~1e-16 numerically) and at Shtanov-Sahni background-coupling level, for any `q(a)`.
- **Limitation:** an isotropic-average result — does not by itself bound the finite-r, single-pair regime (that gap is what `P223`, row 1, closed separately).
- **Reopen only if:** a new population/averaging assumption is introduced.

### 7. Eq.32 numerical match
- **Evidence IDs:** `code/eq32_verify.py`, `CONSILIENCE_eq32.md`, Belle II 2023 measurement.
- **Status:** the NUMBER is VERIFIED (`0.0608%`, `1.00σ`, PDG 2024); the RELATION is NOT SUPPORTED as genuine.
- **Support:** the match itself is `[VERIFIED-BASH]`, re-run directly, real and unexplained.
- **Counterevidence:** three independent methods (formal derivation, statistical null-model, real experimental check) converge on non-support as a genuine relation. Belle II's own 2023 `m_τ` measurement gives `1.84σ` tension with Eq.32's exact prediction — ambiguous, not decisive.
- **Reopen only if:** a real derivation mechanism is found (5 external niches + 3 internal group-theoretic attempts already exhausted, `CLAUDE.md` exclusion zone) — do not re-attempt without a new candidate.

### 8. Bottleneck-1-adjacent: `P158` mass-assortativity / population-averaging
- **Evidence IDs:** `P157`, `P158`, `ADDENDUM2`, `P195`, `P230`, `docs/162` item 9, 6 FINDINGs under `experiments/20260909-tng-mass-assortativity/`.
- **Status:** CLOSED FOR CURRENT SOURCE-FAITHFUL ESTIMAND.
- **Support:** best-powered FLAMINGO estimand (`N=1200`, real spatial jackknife): `rho=-0.0231±0.0476`, robust to `M200c`/`M500c` convention; `P158`'s `rho>-0.5` safety margin doubly confirmed. A real 2PCF-style estimand (replacing nearest-neighbor after `P230`'s ontology correction) reproduces v82's own cited empirical anchor's order-of-magnitude and richness-direction (`r0`); mass-marked `rho(r)` finds no detectable signal in the tested construction.
- **Limitation:** `gamma` (clustering shape) mismatch only PARTIALLY explained — a real `~0.5`-unit residual remains, outside this project's simulation-only toolkit to close further. Ontological/mechanistic interpretation of v82 as a whole NOT closed; cosmological causality NON-IDENTIFIED.
- **Reopen only if:** a new observational cluster sample (not simulation-only), a new v82 interpretation of "node," or TJB input changes the ontology.

### 9. `k` question (`P214`-`P217`)
- **Evidence IDs:** `FINDING_P215_P216_P217_RETRACTION_after_step8a.md`.
- **Status:** all three attempts RETRACTED; narrow survivors only.
- **Support:** `β₁` per-node / `β₂` per-pair asymmetry (`P216` §5) stands. Relativistic Fermi-gas physics in `P215` independently confirmed. v82 explicitly declines a GR analog, assigns binary-pulsar timing to GR's own jurisdiction.
- **Counterevidence:** `P215` inverted its own conclusion (degeneracy is the MOST excluded reading, not least) and reproduced a known factor-of-2 error against a forbidden target (Table A1). `P216` cited only sections where the theory is applied, none where it self-bounds. `P217` swept `β` one-sided (v82 only), stated a two-sided conclusion. Root cause: one-sided search reported as two-sided comparison, across all three.
- **Reopen only if:** the corpus is re-read from both v6 AND v82 symmetrically, with the FL Step -3 pre-work check actually run first.

### 10. Ernest Prabhakar's 6-point critique
- **Evidence IDs:** `FINDING_P166`, `E9`/`P199`, `E19`-`E21`, `E13`/`E15`-`E17`, `e8b400b` (sent), `9987452` (TJB reply).
- **Status:** all 6 items answered; SENT 2026-09-07; TJB acknowledged same day.
- **Support:** real computation per item, Step 8a skeptic pass on every substantive one; 4 of 6 items on TJB's own follow-up revision list trace directly to material this project sent.
- **Counterevidence/limitation:** every substantive verdict is WEAKENED or mixed, not a clean win for either side (`NO_AUTHOR_ERROR`) — none establishes MULTING correct or incorrect.
- **Reopen only if:** TJB or Ernest raises a new, related question.

### 11. Thermal-pair kSZ fingerprint (`docs/161`, §8)
- **Evidence IDs:** `NR-025`, SUTVA fix, synthetic four-world battery, Fork 1a/1b.
- **Status:** between "falsifiable test" and "synthetic validation" — not yet at a real observational test.
- **Support:** fixed-window design killed and replaced by a continuous per-pair `ξ_pred(z,s)` design; a real SUTVA bug (43.2% pair-sharing) found and fixed; synthetic four-world identifiability battery ADEQUATE (MULTING, optical-depth-confounded, merger-confounded, null all distinguished).
- **Counterevidence/limitation:** Fork 1a (Gong/Bean data request) sent 2026-09-11, awaiting reply. Fork 1b Phase 2 (real ACT DR6 + real DESI DR1) not started. A positive result would support this specific fingerprint over tested alternatives — NOT the full IDM construction, isomers, cosmological bridge, or `β1,β2`'s fundamental origin.
- **Reopen only if:** Gong/Bean reply, or Fork 1b Phase 2 is explicitly authorized to proceed.

### 12. H1b — WHIM thermal-mass bias (parked)
- **Evidence IDs:** `parked/H1b-whim-thermal-mass-bias.md`, `P210`-`P212`, `experiments/20260909-tng-whim-pilot/`.
- **Status:** PARKED — one of two real ingredients measured, the correlation itself never run.
- **Support:** `E_WHIM` measured at real `N=71` TNG-300 clusters (`r=-0.429, p<0.001`, mass vs. WHIM%), both positive controls pass 71/71. Two dynamical-state confound proxies tested null.
- **Counterevidence/limitation:** `M_HE` (hydrostatic mass bias, the 4th ingredient) has no public per-cluster catalog — 9-channel search closed, data-request letter sent 2026-09-09, no reply as of last check. The `E_WHIM → delta_M` correlation itself — H1b's own actual test — has not run.
- **Reopen only if:** The Three Hundred Project authors reply with `M_HE` data, or a public catalog is found.

### 13. AVB (adversarial verification benchmark, solving arm)
- **Evidence IDs:** `experiments/adversarial-verification-benchmark/`.
- **Status:** inconclusive, as pre-registered.
- **Support:** full pipeline executed (skeptic + blind Run-3 + K5 reproducibility); a real bug found+fixed in a global hook along the way.
- **Counterevidence/limitation:** `N=25`/`32`, McNemar `p=1.0` — underpowered by design, not a null finding about the treatment-vs-baseline hypothesis.
- **Reopen only if:** the corpus is scaled to the pre-registered power target.

---

## What this Ledger does NOT do

- Re-derive, re-verify, or re-weigh any claim above — every line is a
  pointer into `CURRENT_EVIDENCE_STATE.md`, not new work.
- Establish MULTING's own physical correctness anywhere
  (`NO_AUTHOR_ERROR`) — every row is about this project's own
  reconstruction and its current evidentiary state.
- Replace `CURRENT_EVIDENCE_STATE.md` as the canonical detail source —
  this is the 1-2-page index; that file is the evidence.

## Next step (per this Ledger's own closeout)

Per the `2026-09-13` program decision: no new computation. Next action
is a claim-by-claim adversarial pass on `paper/methodology_paper_
draft_v0.md` (still in its 2026-09-08 cooling-off period, venue
decision joint, not autonomous) — every strong sentence checked against
this Ledger's own status column, old verdict labels not carried
forward, `what was established` / `what was falsified` / `what was
exhausted` / `what remains non-identified` kept separate, and
"campaign stopped" never silently reworded as "question solved."

NOT_VALIDATION · NOT_REFUTATION · OUR_RECONSTRUCTION · NO_AUTHOR_ERROR
