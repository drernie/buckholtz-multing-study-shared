# docs/145 — Мета-аудит проекта: harvest scan + research-audit (полный), 2026-08-17

**Дата:** 2026-08-17
**Инициатор:** пользователь, вне рамок текущей физической работы над Step B —
запрос «какие у нас скилы, что мы упустили, есть ли что-то ценное, чего мы
не признали ценным».
**Метод:** `/harvest scan` (я сам, по контексту сессии) → `/research-audit`
(делегирован general-purpose агенту, полный проход по null_results/,
parked/, pearl_registry/, всем 21 директориям experiments/, docs/,
activeContext.md + архивы).
**Статус:** оба скила отработаны. Это консолидированный отчёт + план.
**Labels:** NOT_VALIDATION · NOT_REFUTATION · OUR_RECONSTRUCTION · L0 descriptive
(мета-документ о процессе исследования, не о физике MULTING)

---

## Часть 1 — Harvest scan (побочные активы)

7 вопросов методологии, ответы по контексту сессии:

1. **Исходная цель:** аудит/реконструкция космологической модели MULTING/IDM
   TJB — оценить научную состоятельность, выйти на конструктивный контакт с
   автором.
2. **Результат по цели:** частично достигнут. Acknowledgment от TJB
   подтверждён; научная публикация/финальный вердикт — нет.
3. **Рабочие скрипты/модули:** ~55 python-скриптов P1-P55 (все ruff-clean, с
   ассертами), reusable helpers (`christoffels_exact`, `ricci_exand`,
   `covariant_div_upper`, `covariant_box`, dimension-tuple helpers из P39),
   book-рендеринг pipeline, digitization/extraction pipeline.
4. **Неожиданные наблюдения:** 11-категорийная таксономия отказов
   self-review; паттерн «внешний якорь уже был в репо» (P53); Table A1 =
   AI-output (не расчёт MULTING); 7:9:17-совпадение (p=0.004) — отдельная от
   Eq.32 находка.
5. **Повторяющиеся ошибки → материал для чек-листа:** тавтологичный
   positive control (P18/19/50B/51/52/54), «silently pick one of multiple
   unresolved readings» (P28/42/50B), non-discriminating substitution-check
   (P54), reuse downgraded prior result at pre-correction strength (P53).
6. **Переиспользуемо прямо сейчас:** FL Full-Ladder + EstimandOps стек
   (уже project-wide через `~/.claude/rules/`), four provenance gates
   (тоже уже project-wide), `covariant_box`/`christoffels_exact` — переносимы
   на любую FRW-perturbation-theory задачу.
7. **Можно показать внешнему человеку:** книга (22 главы, PDF, готова);
   11-категорийная таксономия (методологическая заметка); 7:9:17-находка
   (потенциальная отдельная заметка); `p ∝ size(G)^0.95` look-elsewhere
   поправка (см. ниже — по мнению research-audit это отдельный портируемый
   актив).

### Asset scoring (Reuse+Pain+Proof+Uniqueness, каждый 1–5)

| Актив | Тип | R | P | Pr | U | Score | Действие |
|---|---|---|---|---|---|---|---|
| FL/EstimandOps + Provenance Gates стек | process_asset | 5 | 4 | 5 | 4 | **18/20** | уже promoted project-wide — признать явно как готовый результат |
| 11-категорийная таксономия failure-modes (P21-P55) | process_asset | 5 | 3 | 4 | 4 | **16/20** | **DONE 2026-08-23** — собрана в `docs/146_failure_mode_taxonomy.md` (11 категорий, каждая с 2-4 процитированными примерами, spot-checked) |
| `p ∝ size(G)^0.95` look-elsewhere поправка (research-audit нашёл) | research_asset | 4 | 3 | 4 | 5 | **16/20** | мини-проект: формализовать, проверить на 2-й задаче |
| 7:9:17 numerical coincidence (p=0.004) | research_asset | 2 | 2 | 4 | 5 | **13/20** | отдельная заметка / мини-проект |
| `covariant_box()`/`christoffels_exact()` toolkit | code_asset | 4 | 2 | 5 | 2 | **13/20** | сохранить как модуль |
| Book rendering pipeline | code_asset | 3 | 2 | 5 | 2 | **12/20** | сохранить как модуль |

---

## Часть 2 — Research-audit (полный, все 5 фаз)

*Ниже — вывод агента, без сокращений по существу (структура сохранена).*

### Phase 0-1 — структурный скан

- `null_results/`: 17 записей (NR-001…NR-018, NR-006 отсутствует — разрыв
  без объяснения, не критично, но стоит закрыть примечанием)
- `parked/`: 1 запись (NW-001)
- `pearl_registry/INDEX.md`: 162 строки, **484 KB** — файл плотнее, чем
  инструмент чтения ожидает построчно (см. Gap ниже)
- `experiments/`: 21 директория — 1 гигантский трек (`20260803-bridge/`,
  P1-P55) + 14 классифицированных треков + 6 одноразовых скриптов
- `docs/`: 96+ нумерованных файлов

### Phase 2 — хронологическая карта (сжато, полная версия — в выводе агента выше)

**null_results (Track A, мост F→H_MULT(z) + смежные):** NR-001…NR-018,
все REJECT/KILL/WEAKENED, последовательно закрывающие constant/power-law
мост, PS/virial k_A(z), H1a-e mass-bias конфаундеры, S³-геометрию Eq.32,
Shtanov-Sahni bridge, 7:9:17 RG-boundary, naive pair-fluid virial mapping.

**Bridge-трек (P1-P55):** НЕ цепочка отказов, а непрерывная деривационная
цепь (build→skeptic→correct→register→commit). Из 55 шагов: большинство
(35+) — «corrected, не true-kill»; **P51 и P52 — два подлинных true-kill**
(редкое и ценное свидетельство некоммитированности к rubber-stamp); P53 —
пример правильно сработавшего stop-rule; P54/P55 — Step B в процессе.

**14 прочих треков:** eq32-structural-pearl (open/promoted-with-caveats),
f4-eq32-synthesis (rejected-mechanism), 4× H1-конфаундер-треки (killed),
h1b-whim (**externally blocked**, ждёт TNG/300-доступ), llm-consensus
(open pilot), ksz-force-law (**parked, несмотря на 10/10 PASS собственного
гейта**), eq32-look-elsewhere (closed/descriptive), ncg-spectral-triple
(paused/mixed), zenodo-archive (**closed/reproduced, 94/94**),
two_component_eps (explicitly open, anti-zombie заглушка).

### Phase 3 — паттерны

**Паттерны отказов:**
1. «Совпадение без механизма» повторяется **трижды** (S³→F₄/G₂/J₃(O)→
   "12=dim(gauge)"), каждый раз тот же provenance-провал (post-hoc
   relabeling), но не консолидировано в одну запись.
2. H1a→e конфаундеры убивались последовательно, но итоговый вывод оказался
   МЕНЕЕ устойчив, чем казалось на каждом шаге (NR-015 переквалифицировал
   «survives 3 confounds» как overclaim после T_X).
3. Тавтологичные self-checks — системный паттерн и в bridge-треке, и вне
   его.
4. Reuse результата без re-verification под другую геометрию (P36 — самая
   тяжёлая коррекция кампании).

**Что подтверждено (ядро):** мост F→H_MULT(z) не существует в
опубликованном виде (структурный факт); Table A1 = AI-output; диполь/
квадруполь не превосходит monopole-only на реальных данных (R011); Zenodo-
архив воспроизведён точно; γ(a,k)=1 no-slip (P49, честно помечен как
textbook-результат); P51/P52 true-kills.

**Zombie-риски:** Eq.32-механизм-охота через exceptional Lie groups —
не полноценный зомби (каждая попытка получила свой вердикт), но
распылена по 3+ файлам без единой консолидированной записи с условием
остановки. Настоящих активных зомби на уровне null_results/parked — нет.

### Phase 4 — Gap-анализ

- **Символ κ/A/g** — абсолютный масштаб κ никогда не зафиксирован нигде в
  проекте; пять отдельных находок (P14§6/P17/P21/P22/P26) переоткрывали
  «тот же» пробел независимо, пока P27 их не объединил.
- **DESI-эры (2024+) fσ8/RSD-ограничения на Δ_G/G_N** — явно названный
  P31 следующий шаг, до сих пор не выполнен (используется только
  Bean&Tangmatitham 2010).
- **Pearl Registry:** нет просроченных `next_check` (все в диапазоне
  2026-09-01…2027-06-01). Локальная схема `R=X P=Y` не соответствует
  глобальному протоколу `impact_score` (`~/.claude/rules/falsification-
  ladder.md`) — расхождение, не критичное, стоит унифицировать.
- **Кандидаты на «нераспознанную standalone-находку»:** `p ∝ size(G)^0.95`
  look-elsewhere поправка (явно помечена автором «переносимо за пределы
  физики», никогда не оформлена отдельно); 11-категорийная таксономия
  self-review отказов; LLM-consensus specification-ablation pilot (row 42
  pearl registry); LABELS-01 stopping-rule insufficiency (row 23).

### Phase 4.5 — внешние блокеры

| Блокировано на | Независимая альтернатива есть? |
|---|---|
| Ответ TJB (Q1/Q2/Q3, k-определение, β_d/β_q) | Частично (P7 нашла текстуальное разрешение) |
| H1b: TNG/The Three Hundred доступ | Нет |
| DESI DR2 (N_opt significance) | **Да** — другие публичные fσ8/RSD-компиляции доступны сейчас |
| Belle II m_τ (~2026-27) | Нет |
| Simons Observatory ΔN_eff | Нет |
| ksz-force-law: независимый второй ридер | Нет — нужен другой человек/модель |

### Phase 5 — стратегический вердикт

**Q1 (falsifiable?):** Да — Zero-Signal Gate проходится системно, 17
REJECT + 2 true-kill доказывают, что проект получает и принимает
отрицательные результаты.

**Q2 (направление оправдано?):** Смешанно. Методологическая дисциплина
кампании P1-P55 — эталонная. Но по существу: 55 шагов с 2026-08-03 не
произвели ни одного нового falsifiable числа за пределами мягкого потолка
`Ag²≲8.4e-12`, установленного ещё на P22 (2026-08-13) — все шаги после
либо уточняли нормировку, либо доказывали её принципиальную
неопределимость (P51/P52). Это легитимный исход (proof of
underdetermination, заявленная в PLAN.md конечная точка B), но кампания
ещё не объявила его явно, продолжая наращивать шаги.

**Q3 (активные зомби?):** Один незакрытый риск — Eq.32-mechanism-hunt
(не зомби формально, но не консолидирован).

**Q4 (дешевейший следующий тест):**
- *Независимо сейчас:* (1) DESI-эры литературный поиск по Δ_G/G_N
  (дешевле очередного internal-normalization прохода); (2) консолидация
  Eq.32-mechanism-hunt в одну null_results-запись (полдня); (3) B2 Part 2
  (уже в движении).
- *Externally blocked без альтернативы:* Belle II, Simons Observatory,
  H1b WHIM-доступ, kSZ v7 второй ридер.

**Q5 (что иначе с нуля):** (1) реестр символов/параметров с P1, не
ретроактивно на P20; (2) единый реестр «числовых совпадений в теории
групп» с начала Eq.32-охоты; (3) H1-конфаундеры как совместная
multivariate-регрессия с первого шага; (4) **pearl_registry разросся до
операционной проблемы** (484KB/162 строки) — сам этот аудит потратил
основную часть усилий на его чтение чанками.

### Итоговая оценка агента: **7/10**

За: редкая дисциплина честного самоопровержения, работающий stop-rule,
ноль активных зомби. Против: 2.5 месяца, ~55 шагов — центральный вопрос
проекта там же, где на NR-001; операционная энтропия (pearl-реестр,
распылённая Eq.32-охота) начинает конкурировать по стоимости с самой
физикой.

---

## Часть 3 — Консолидированный план действий

Приоритет по (дешевизна × информационная ценность), не по хронологии:

### Немедленно, дёшево (< 1 день каждое)

1. ~~Консолидировать Eq.32-mechanism-hunt в одну `null_results`-запись~~
   — **DONE** (уже была выполнена 2026-08-17, `null_results/20260817-
   nr019-lie-group-numerology-eq32-mechanism-consolidated.md`, confirmed
   при gate-check 2026-08-23). Zombie-риск №1 закрыт.
2. ~~Явно объявить или продлить P52's stop-rule~~ — **DONE 2026-08-23**,
   `docs/147_campaign_stop_rule.md` (GO/STOP/REDIRECT/PROMOTE критерии +
   3 поимённых bottleneck'а + явный запрет продолжать уже закрытый
   `[WS: derive-amplitude-mechanism]` без нового факта/механизма/
   противоречия).
3. ~~Собрать 11-категорийную таксономию failure-modes~~ — **DONE
   2026-08-23**, `docs/146_failure_mode_taxonomy.md` (11 категорий,
   независимый синтез общим агентом, spot-checked против исходников).

### Дёшево, но требует диспетчеризации (несколько дней)

4. ~~DESI-эры (2024+) литературный поиск~~ — **DONE 2026-08-24**,
   `FINDING_P132_desi_era_growth_rate_bound_check.md`: NULL RESULT,
   существующий потолок P22/P31 (`A·g²≲8.39×10⁻¹²`) остаётся самым
   жёстким — все проверенные DESI-эры источники (2024-2026) шире в
   ~40-50 раз. Валидный, не провальный результат.
5. **Формализовать `p ∝ size(G)^0.95` look-elsewhere поправку** как
   портируемый метод, проверить на второй, не-Eq.32 задаче (тест
   Uniqueness=5 из harvest-скоринга).

### Среднесрочно

6. ~~Unify pearl_registry схему~~ — **DONE 2026-08-23, header-only**:
   238 существующих строк НЕ переписаны физически (26 из них содержат
   экранированные `\|` в математической нотации — наивный скрипт по
   числу колонок их бы повредил; риск, реальность которого подтверждена
   прямой проверкой, не предположена). Вместо этого — документированная
   каноническая интерпретация `R=X P=Y → impact_score:=R+P` +
   forward-only 8-колоночное правило (matches falsification-ladder.md
   exactly) для всех новых строк, + forward-only 400-600-символьное
   ограничение. См. заголовок `pearl_registry/INDEX.md` целиком.
7. **Продолжить B2 Part 2** (пространственный `Q^i`, continuity+Euler) —
   легитимно, но НЕ приоритет №1 по факту Q2-вердикта; держать в очереди
   после пп. 1-5.

### Заблокировано, без действия сейчас

Belle II m_τ, Simons Observatory ΔN_eff, H1b WHIM-доступ, kSZ v7
независимый второй ридер — фиксировать как EXTERNALLY_BLOCKED, не тратить
циклы.

---

## Часть 4 — Какие скилы использовать дальше

| Скил | Когда | Зачем |
|---|---|---|
| `/gate-check` | перед тем как выбрать между п.1-5 выше | 10-вопросный GO/STOP/REDIRECT, формализует приоритет |
| `/hypothesis-arbiter` | если решать между «продолжать B2» vs «пивот на DESI-поиск» | конкурирующие направления, а не гипотезы — но метод применим |
| `/cross-domain` | если DESI-поиск или Φ,Ψ-extension B2 упрутся в стену | неочевидный мост из другой области, когда стандартный путь исчерпан |
| `/snr` | когда список открытых нитей (п.1-7 выше + H1b + ksz-v7 + Eq.32) станет неуправляемым | приоритизация по сигнал/шум |
| `/harvest promote` | для `p ∝ size(G)^0.95` и 11-категорийной таксономии, после п.3/5 | конкретный план развития актива с Score 13+ |

**Не требуется прямо сейчас:** повторный `/research-audit` (сделан
полно, следующий имеет смысл через месяц активной работы или после
крупного pivot); `/session-retrospective` (это аудит одной сессии, не
всего проекта — другой масштаб).

---

## Часть 5 — Update, 2026-08-23: Q2's "no new falsifiable number" is now stale

**Что изменилось.** Часть 2/Phase 5 (Q2) выше утверждала: «55 шагов с
2026-08-03 не произвели ни одного нового falsifiable числа за пределами
мягкого потолка `Ag²≲8.4e-12`, установленного ещё на P22» — верно НА
2026-08-17. Между 2026-08-17 и 2026-08-23 кампания продолжилась (P123-
P131, `experiments/20260803-bridge/`) и **эта конкретная фактическая
претензия больше не верна** — новые falsifiable числа/результаты
появились. Остальная часть Phase 5 (Q1/Q3/Q4/Q5, итоговая оценка 7/10)
здесь не пересматривается — это отдельная, более узкая коррекция.

**Что именно произвели P123-P131** (все `[VERIFIED]`, каждое со своим
`FINDING_P<n>.md`, ruff+881 tests на каждом коммите):

| # | Число/результат | Верно ли, что это НОВОЕ falsifiable число |
|---|---|---|
| P125 | `q≈0.465960` (self-consistent tail exponent, разрешило P122's power_law vs power_law_log двусмысленность) | Да — новое число, не переоценка P22 |
| P126 | `A_hat=6678.998` (affine factorization k=0.3↔k=0.5, spread<0.01%) | Да — но исправлено skeptic'ом с "independent match" на "affine factorization" |
| P127 | reduced late-time system, affine mechanism confirmed | Механизм, не число само по себе |
| P128 | q≈0.466 воспроизведено из REAL hand-off state, точность 6e-5 | Да — закрывает конкретный gap P127 |
| P129 | попытка вывести A=6679 из hand-off; skeptic поймал overclaim | Отрицательный/методологический результат |
| P130 | `N_eq=11.512925` (closed-form, из C_MATTER/Lambda_cc) | Да — точное аналитическое число, не read off траектории |
| P131 | `VPP-ONLY-REFUTED` (ratio 1.00 и 0.89 против A_hat=6679) | Да — чистый отрицательный результат causal ablation |

**Итог коррекции:** P123-P131 закрыли конкретную подветвь «откуда берётся
A≈6679» с честным, хорошо охарактеризованным ОТКРЫТЫМ статусом
(genuinely coupled/phase-dependent эффект, не сводится к одному
доминирующему члену — установлено ablation'ом, не предположено). Это НЕ
меняет центральный факт: мост F→H_MULT(z) по-прежнему не установлен в
опубликованном виде (структурный вывод Phase 3 выше остаётся в силе). Но
конкретная фраза «ни одного нового falsifiable числа» была фактически
неверна для периода 2026-08-17→2026-08-23 и требовала исправления, а не
молчаливого устаревания — отсюда эта часть.

**Методологическая заметка, отдельная от физики:** P129→P131 — чистая
иллюстрация Context Asymmetry Rule (`falsification-ladder.md`) в
действии дважды подряд в одной сессии: P126's overclaim пойман
skeptic'ом, P129's overclaim пойман skeptic'ом снова (другой механизм
той же категории — тавтологичная переформулировка вместо независимой
проверки), и урок применён ПРОАКТИВНО в P131 (спланирован как ablation,
не переформулировка, ДО того как построен, а не после того, как ещё раз
пойман). Это ровно то самостоятельное самокоррекция, которую Часть 2
выше называет «редкой дисциплиной честного самоопровержения» — новый,
свежий пример той же самой сильной стороны кампании.

---

## Часть 6 — Round-2 bottleneck campaign closure (2026-08-24)

### Цель

После первого strategic arbiter и housekeeping-паузы были повторно атакованы
оставшиеся bottleneck'и bridge-to-observables кампании. Целью было не
"продолжить вычисления", а определить, появился ли хотя бы один новый
различающий механизм, оправдывающий reopening ранее заблокированных веток.

### Итог по bottleneck'ам

1. External / bridge bottleneck:
   остаётся BLOCKED. Нового внешнего факта, который разрешал бы переоткрыть
   уже исчерпанную прямую F→H_MULT(z) линию, не появилось.

2. IC-robust observable / IC-sensitivity bottleneck:
   остаётся GENUINELY OPEN.

   Три конкретных механизма/дизайна были проверены:

   - P135: pole-based explanation — REFUTED.
   - P136: early-transient-amplitude explanation — REFUTED.
   - P138: horizon-relative anchor — DESIGN-LIMITED.

   P138 установил:
       aH(t=1) ≈ 1.84

   и поэтому внутри доступного численного диапазона полноценное horizon
   crossing имеется только для k=1. Уже при старте:

       k/aH(1):
       k=1  -> 0.54
       k=2  -> 1.09
       k=3  -> 1.63
       k=10 -> 5.43

   Следовательно k=2,3,10 уже sub-horizon при t=1, и предложенная схема
   "измерять observable через фиксированное число e-folds после crossing"
   не может быть симметрично применена ко всем четырём модам.

   Наблюдаемое соответствие между k/aH(1) и порядком IC-чувствительности
   остаётся только наводящим наблюдением; механизм не установлен.

3. Structural-normalization bottleneck:
   остаётся STRUCTURALLY BLOCKED. Нового независимого relation, снимающего
   идентификационную недоопределённость, в этом раунде не получено.

4. P137:
   отдельная проверенная ветка вернула NULL; она не создала нового основания
   для reopening заблокированных направлений.

### Что исключено

Round-2 не установил механизм IC-чувствительности, но сузил пространство
кандидатов: pole, simple early-transient amplitude и straightforward
horizon-crossing anchoring больше не являются поддержанными объяснениями
в проверенной постановке.

Это отрицательный, но различающий результат.

### Gate verdict

STOP / SYNTHESIZE.

Три последовательные попытки на одном IC-observable bottleneck не дали
нового механизма, причём третья оказалась design-limited ещё до полноценного
physics test. Продолжение четвёртой incremental-попыткой создаёт высокий
Ruling-Theory / momentum risk.

Backward extrapolation of aH(t) to t<1 PARKED, not rejected.
Причина: потенциально проверяемо, но требует экстраполяции за
верифицированный численный домен и пока не имеет достаточного ожидаемого
information gain.

### Reopen conditions

Round-2 reopening разрешается только при хотя бы одном из условий:

- появляется новый механизм IC-чувствительности, не эквивалентный
  pole / early-transient / horizon-anchor гипотезам;
- появляется новый внешний VERIFIED-REAL факт, меняющий один из
  заблокированных bottleneck'ов;
- появляется принципиально новый observable/estimand с заранее
  определённой construct-validity связью с claim;
- пользователь явно санкционирует exploratory fourth attempt с пониманием
  текущего diminishing-return риска.

Иначе новые P-скрипты по этой линии не запускать.

### Workstream verdict

[WS: round-2-strategic-arbiter] CLOSED

Эпистемический итог:
- два bottleneck'а закрыты/заблокированы на текущем information set;
- один bottleneck остаётся genuinely open;
- три конкретных объяснения/дизайна для него исключены или оказались
  нетестируемыми в текущем домене;
- нового PROMOTE-tier physics claim этот раунд не дал.

---

## Часть 7 — Round-3: macro-locality + parallel harvest/audit, 4th mechanism
## exhausted, session closed honestly (2026-08-24, same day)

### Что произошло

После round-2's закрытия пользователь попросил продумать более широкую
skill-архитектуру. Запущено два трека параллельно:

**Трек B (фоновый агент, harvest + research-audit, свежий взгляд).**
Полный отчёт: см. `pearl_registry` пробел ниже. Ключевые находки:
1. **Pearl-registry health-hook имеет реальный, самодиагностированный, но
   не исправленный баг** — `research_health_loop.py` разбивает строки по
   литеральной `" | "`, ломаясь на escaped `\|` в математической нотации.
   Проект сам это диагностировал в `DIAMOND_SCAN_P79_P83.md` (2026-08-21,
   «37 из 181 строк невидимы для health hook»), но 2026-08-23's
   schema-unification был намеренно header-only и НЕ чинил read-side баг.
   Найдено: 6 pending строк с уже просроченным `next_check` сейчас
   невидимы этому хуку.
2. **P133's exponent-vector критерий — НЕ новая математика** [INFERRED/
   WEAK, не проверено полным литературным поиском]: та же техника, что
   Buckingham Π dimensional analysis и structural identifiability
   power-law моделей (Bellman/Åström-era). Ценность — не в теореме, а в
   **дисциплинированной упаковке** (positive control + cross-check),
   которая переносима в другие проекты пользователя с
   multi-parameter-degeneracy задачами.
3. **P134/136/137/138 не имеют записей в pearl_registry** — коммит
   "record P134/P135" не пережил себя для P134 конкретно; дешёвый
   backfill, не сделан.
4. **Zombie-check: чисто.** Ни один из 3 round-2 механизмов не повторяет
   более раннюю (P74-P92) попытку под другим именем.
5. **docs/147 stop-rule сработал** — 15/20 portable process asset,
   рекомендован к продвижению как reusable паттерн.
6. **Именован 5-й, явно спекулятивный кандидат** для bottleneck 2 — P82's
   "common envelope `A(η)`" route (собрал коллапс 0.390, не чистую
   единицу, для родственного но не идентичного вопроса) — НЕ
   верифицирован даже самим агентом, флагирован для справки, не
   действие.

**Трек A (macro-locality → 4-й кандидат-механизм для bottleneck 2, сам
исполнитель).** `macro-locality` skill: boundary audit нашёл, что gauge
choice (`conformal-Newtonian/longitudinal`, `FINDING_P49`'s собственное
«implicitly used throughout») — неисследованная посылка всей цепочки
`P38→P79`. Вердикт: `PART-OF-MACROSYSTEM`, `SURVIVING_HYPOTHESIS=M3`
(gauge artifact). Прогнаны дополнительно `novelty-assessment` (NOVEL —
единственное упоминание в pearl_registry/null_results — тот же P49
caveat, никогда не тестировался как активная гипотеза), `decision-gate`
(ACCEPT, score 7.6/10), `claim-decomposer` (нашёл `C3` — общий физический
факт про gauge artifacts near horizon crossing — как `[MEMORY]`,
требующий верификации до траты усилий), `pareto-leverage-scan` (ACT,
строить сейчас параллельно Треку B), `multi-lens` (альтернативные линзы:
probabilistic/ensemble-averaging framing, multi-stability/bifurcation —
названы, не преследованы).

**`FINDING_P139` (без нового `.py` — code-tracing, не численный
эксперимент).** `C3` верифицирован внешне (`WebSearch`, textbook-уровень:
«density contrast grows in synchronous gauge but remains constant in
conformal-Newtonian gauge before horizon crossing» — Bardeen 1980).
Дальше — три независимые линии рассуждения, прослеженные до
первоисточников кода (не по памяти — первая попытка вспомнить знак в
формуле `qm` дала неверный знак, пойманный прямой проверкой `P73`'s
собственного кода):
1. `contrast()`'s формула (`δρ_m − 3H·Q_m`, `qm`≡`Q_m` подтверждено через
   `P73`'s `dq_tot=-φ̄̇δφ+Q_m`) структурно совпадает со стандартной
   comoving-gauge density contrast — известной в цитируемой этим же
   проектом (P49) литературе (Amendola coupled-quintessence) как
   kinematically gauge-invariant.
2. Newtonian gauge — **полностью** фиксированная калибровка (обе
   скалярные gauge-степени использованы `B=E=0`), в отличие от
   synchronous — генерически нет остаточной свободы такого рода.
3. `Φ=Ψ` — установленный физический результат (`P49`, zero anisotropic
   stress), не свободный gauge-выбор; под общим `ξ⁰` трансформируются
   по-разному, значит сохранение `Φ=Ψ` уже съедает почти всю freedom,
   которую искала гипотеза.

**Вердикт `FINDING_P139`: `M3-WEAKENED-BY-REASONING`** — не формально
опровергнута (полный from-scratch символьный вывод для ИМЕННО этой
M(φ)-связи признан слишком рискованным для завершения безопасно в этой
сессии), но три независимые линии рассуждения сходятся в одном
направлении.

### Итог: 4 кандидата-механизма исчерпаны в этой сессии

| # | Механизм | Вердикт |
|---|---|---|
| 1 | Pole at anchor (P135) | REFUTED, positive-control-tested |
| 2 | Early-transient amplitude (P136) | REFUTED, positive-control-tested |
| 3 | Horizon-crossing anchoring (P138) | DESIGN-LIMITED, не REJECT |
| 4 | Gauge artifact (P139) | WEAKENED-BY-REASONING, не formal REFUTED |

5-й кандидат (P82-envelope, Track B) назван, явно спекулятивен, не
преследован — пользователь решил зафиксировать честно и остановиться,
а не гнаться за пятой попыткой.

### Bottleneck 2 итоговый статус

**GENUINELY OPEN**, с 4 явно исключёнными механизмами и переиспользуемым
знанием о том, ПОЧЕМУ каждый не сработал (а не просто «не сработало»).
Реоткрытие требует: генуинно новый механизм (не эквивалентный этим 4),
новый внешний факт, или явное согласие пользователя на 5-ю попытку зная
diminishing-return паттерн.

### Отдельные housekeeping-находки Трека B (не физика, не действие в этой сессии)

- Health-hook parsing bug — самодиагностирован 2026-08-21, не исправлен;
  затрагивает НЕ только этот проект (глобальный хук).
- P134/136/137/138 отсутствуют в pearl_registry — дешёвый backfill.
- docs/147 stop-rule — кандидат на продвижение в reusable паттерн.

Ничего из этого не выполнено в рамках этой сессии — зафиксировано для
будущей ссылки, по решению пользователя не действовать сейчас.

---

## What this does NOT establish

1. Не решает сам физический вопрос (существует ли мост F→H_MULT(z)) —
   мета-документ о процессе, не о физике.
2. Оценка 7/10 — оценка полноты исследовательской программы (Score
   методологии research-audit), не оценка правильности гипотез MULTING.
3. Список «дешевейших следующих тестов» — рекомендация, не команда;
   решение о приоритете — за пользователем.
4. Per NO_AUTHOR_ERROR: наблюдения о процессе ЭТОГО проекта, не оценка
   работы TJB.
