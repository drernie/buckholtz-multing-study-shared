# FINDING P148 — retrospective promotion of the 2026-08-10..12 scalar
# completion cluster: PROMOTE-WITH-QUALIFIERS

**Date:** 2026-08-26
**Labels:** NOT_VALIDATION · NOT_REFUTATION · OUR_RECONSTRUCTION · L0 mixed
(retrospective audit, not new derivation)
**Status:** `PROMOTE-WITH-QUALIFIERS` — recorded here, in the status line
itself, per the skeptic's own explicit warning against qualifier drift
(§0 below).
**Origin:** after P143–P147 exhausted every checked vector-mediator
construction (each failing for a different structural reason), a prior
cluster of findings from 2026-08-10 through 2026-08-12 was surfaced as a
Novelty-Check discovery: `two_charge_completion.py`,
`FINDING_two_charge_completion.md`, `two_field_action_closure.py`,
`FINDING_P1_two_field_closure.md`, `FINDING_P4_two_field_does_not_rescue_background.md`,
`FINDING_dipole_shell_is_a_double_layer.md`,
`P11_yukawa_screening_breaks_double_layer.py`,
`FINDING_P11_yukawa_screening_breaks_double_layer.md`. This file records
the user-directed retrospective promotion-gate decision after an
independent context-asymmetric skeptic review of all 5 claimed atoms plus
a Recomposition Check.

---

## 0. Why the status line itself carries the qualifiers

The skeptic's own closing warning: *"If Q1–Q5 stay only in the finding
files' §'What this does NOT establish' sections, promotion will drift
over time: readers who see the label without opening the files will
inherit the recomposition's smuggled assumptions."* This file exists
specifically so `PROMOTE-WITH-QUALIFIERS` is never quoted standalone
without its qualifiers attached — matching this project's own repeated
lesson (evidence-inflation pattern, `feedback_evidence_inflation.md` in
global memory) about markers surviving detached from their scope.

## 1. The promoted core (what survives independently)

- A canonical, healthy scalar action exists:
  `S = ∫d⁴x[½(∂φ)²] + Σᵢ∫dτ[gmᵢ + pᵢ·∇]φ(xᵢ)`, `pᵢ=κkᵢrᵢ/c²` — correct
  kinetic sign, massless, Hamiltonian bounded below (Atom 3: `CONFIRMED`).
- It reproduces MULTING's tier *structure* — three bilinears of two
  charges `(m,q)`, alternating `(+,−,+)` signs, `1/r²,1/r³,1/r⁴` — as
  algebra, not by hand (Atom 1: `CONFIRMED`, structural scope only —
  see §2).
- The exact cosmological background cancellation for the massless
  mediator is a rigorous consequence of Newton's shell theorem, not a
  numerical artifact (Atom 5, massless case: `CONFIRMED`).

## 2. The five qualifiers (Q1–Q5) — mandatory whenever this status is
## cited, per §0

**Q1 (Atom 1 scope).** Reproduces MULTING's parametric tier *structure*
and sign pattern — **not** its fitted numerical ratio. This
construction gives `β_q/β_d = √6/2 ≈ 1.2247`; MULTING's own AI-fitted
value (Gate 2, not a measurement) is `4.0` — a `×3.3` mismatch. "Exact
reproduction" applies to form, not number.

**Q2 (Atom 2 — reformulation, not derivation).** The `(+,−,+)` sign
pattern is the binomial expansion of `(m−κk)²` — real algebra — but the
`(m−κk)` structure itself requires two matching-MULTING choices
(mirror-symmetric orientation, chosen to match `F_d`'s A↔B symmetry;
`q` opposite-signed to mass, chosen to make `F_d` repulsive). This is a
reformulation of MULTING's own sign postulate as an equivalent bilinear
postulate, not an independent, first-principles derivation of it.

**Q2b (recomposition tension, new this review).** Mirror-symmetric
orientation is physically an *induced* polarization configuration. But
`docs/131`'s own sombrero analysis shows that for an *intrinsic* dipole
(exactly what `P1`'s action uses — `pᵢ=κkᵢrᵢ/c²` is a fixed property of
body `i`, not a gradient-response) the mirror-symmetric alignment is the
**unstable energy maximum**, not the stable minimum. `FINDING_P4`
already surfaced this mismatch; it remains unresolved here.

**Q3 (Atom 4 — narrow, not general, EP escape).** The "fifth force, not
gravity" reclassification is logically valid for `docs/131`'s
*specific* argument (it names and relaxes the assumption "`F_oP` is
pure gravity"). It does **not** establish compatibility with actual
laboratory/solar-system fifth-force bounds (MICROSCOPE, Lunar Laser
Ranging). "EP-resolved" means the narrow structural objection is
answered — not that experimental viability is established.

**Q4 (Atom 5 — P11 mechanism is a candidate, not a result).** The exact
massless-case cancellation is real. `P11`'s proposed fixed-mass
(`μ~H₀/c`) mechanism for breaking that cancellation cosmologically while
preserving it at cluster scale is an outlined candidate with two named,
unresolved items: whether the near-field `β_d=2, β_q=√6` ladder survives
a nonzero `μ` at cluster scale, and whether `μ~H₀/c` specifically gives
an observable magnitude. **Addressed below as P149.**

**Q5 (recomposition — trade-off glossed by the combined label).**
"Cosmologically null" (needs `μ=0` exactly) and "mass-scaling-corrected
cosmological signal" (needs `μ~H₀/c` nonzero) are two different
operating points, not simultaneous properties of one construction. The
honest combined statement is conditional: null at `μ=0`; a candidate
signal only if a nonzero `μ` is shown compatible with the near-field
structure (P149) and its magnitude is checked (not yet done).

## 3. Recorded status (per user's own formulation, verbatim)

> Scalar completion promoted as a mathematically healthy local
> structural realization, not as a validated MULTING completion. It
> reproduces the tier hierarchy structurally, but not the fitted
> numerical ratio `β_q/β_d`; its required mirror/sign choice is not
> dynamically derived and is in tension with the intrinsic-dipole
> stability analysis; its fifth-force reinterpretation removes the
> narrow gravitational-EP objection but has not yet been confronted
> with laboratory/solar-system fifth-force constraints; its exact
> cosmological cancellation is established only for the massless
> mediator.

Compressed one-line status for indexing:
**mathematically viable local scalar realization, physically
conditional, cosmologically unsuccessful in the exact massless case.**

## 4. Contrast with the vector-mediator chain (P143–P147)

Where every checked vector construction failed outright (wrong power
law, inadmissible operator, non-generic tuning required), the scalar
route's *local* structure genuinely works — this is a materially
different, more positive outcome, not another null result. What remains
open is not "does a healthy local completion exist" (yes) but "does it
survive contact with (a) near-field mass-scale perturbation and (b)
real experimental fifth-force bounds" — a narrower, more answerable
question.

## 5. Next steps, in the order construct-validity requires (not the
## skeptic's original order — see §6)

1. **P149 — Q4, massive-mediator near-field hierarchy test.** Internal
   mathematical consistency: does `β_d(μr), β_q(μr)` stay close to
   `β_d(0)=2, β_q(0)=√6` at cluster scale for `μ~H₀/c`? Cheap, no
   external data needed.
2. **P150 — Q3a, fifth-force experimental mapping gate.** Construct
   validity, before any numerical bound: does MULTING's own charge
   `pᵢ=κkᵢrᵢ/c²` even map onto a real fifth-force "composition-dependent
   charge" observable that MICROSCOPE/LLR-type experiments measure? Not
   assumed — checked against MULTING's own established definitions.
3. **P151 — Q3b, only if P150 returns `MAPPING-VALID`.** Compare the
   properly-derived differential-acceleration observable
   `Δa ∝ q_source(q_A/m_A − q_B/m_B)K(r)` against the real experimental
   bound — not `κ` plugged directly into `η_Nordtvedt`.

## 6. Why this order, not the skeptic's original Q3-then-Q4 order

The skeptic proposed Q3 (Nordtvedt/MICROSCOPE) as the higher-value first
step. **Overridden by user review**: plugging `κ` directly into the
Nordtvedt parameter or a MICROSCOPE bound without first deriving the
mapping from MULTING's own `pᵢ=κkᵢrᵢ/c²` to a real test-mass
composition-dependent charge would repeat exactly the "similar
magnitude ⇒ assumed same observable" error this project has repeatedly
learned to avoid (`artifact-provenance-gates.md` Gate 2 spirit — a
target's provenance must be established before comparing against it).
Corrected order: internal consistency (P149) → observable mapping
(P150) → external constraint (P151), never external-constraint-first.

## What this file does NOT establish

1. Not a claim about MULTING itself (Gate 1) — entirely this project's
   own reconstruction cluster, now re-evaluated.
2. Does not run P149/P150/P151 — framing and status recording only.
3. Does not resolve the Q2b mirror-symmetric/intrinsic-dipole stability
   tension — recorded as open, not attempted here.

## Update, 2026-08-26 (same day) — P149 (Q4) completed and skeptic-corrected

`P149` executed and independently re-reviewed by a second skeptic pass.
**Result: `MASSIVE-MEDIATOR-NEAR-FIELD-RATIO-INVARIANT-COMPATIBLE`** (label
corrected from an initial `...-NEAR-FIELD-COMPATIBLE` — "the ladder
survives" overclaimed; only the convention-independent ratio invariant
`Λ(s)` was actually tested, per `P1`'s own finding that only the ratio is
physical). Deviation from `Λ=3/2` is `10⁻⁷`-`10⁻⁴`% at cluster scale
(where `β_d=2, β_q=√6` was derived), growing to `~15%` only at the Hubble
radius and asymptoting toward a bounded `~33%`, never diverging.

The skeptic's own proposed stress-test on the ratio-only reduction (could
`β_d(μ)`, `β_q(μ)` individually drift while cancelling in the ratio?) was
answered in the strongest possible direction: `Λ`'s own drift is `O(x²)`,
*larger* than either individual tier's own drift (`O(x³)`, `O(x⁴)`) —
the ratio-only check is the *strictest* available, not a looser one that
could hide drift.

A real, if numerically inconsequential, math error was also caught and
fixed: the small-`(μs)` expansion quoted from `P1`'s own script was
missing a real `+(μs)³` term (P1's own `sp.series` truncated before that
order) — corrected in both `FINDING_P149` and (with a dated correction
note, not silently) in `FINDING_P1_two_field_closure.md` itself.

**Q4 status: `RESOLVED`, precisely scoped.** Next: `P150` (Q3a, fifth-
force experimental mapping gate) — does MULTING's own `p_i=κk_ir_i/c²`
even map onto a real composition-dependent fifth-force observable before
any numerical bound is attempted, per the user's own corrected ordering.

## Update, 2026-08-26 (same day) — P150 (Q3a) drafted, skeptic review pending

`P150` (`FINDING_P150_fifthforce_mapping_gate.md`) works through the
user's own 7 sub-questions for the fifth-force experimental mapping gate.
Found MULTING's own preprint supplies no formula for `k_A` applied to
ordinary matter (already flagged by this project's own earlier audit,
`docs/29/30/34` — author question sent, never answered). This project's
own one prior attempt to extend `k_A` beyond clusters (`docs/118`,
2026-06-17, `k_Sun=E_corona/c²`) does not generalize to laboratory test
masses (solids have no corona). A more literal reading (bulk internal
thermal/vibrational energy, worked out fresh here) does generalize and
gives a real, composition-dependent (~4× Ti vs Pt, from atomic mass
alone) MICROSCOPE-relevant signal. **Verdict: `MAPPING-AMBIGUOUS`** —
two internally-coherent readings diverge sharply on whether `Q3a` even
clears; neither is licensed by MULTING's own text as the intended one.

Skeptic review dispatched — specifically asked to stress-test whether a
THIRD reading (virial-type internal kinetic energy, which for a rigid
solid could be argued to be ~zero, unlike a gas cloud's constituent
motions) would favor `MAPPING-UNAVAILABLE` more decisively than the
finding's "ambiguous, two readings" framing allows — result pending.

## Update, 2026-08-26 (same day) — P150 skeptic-corrected: third reading
## added, verdict rebalanced

Skeptic review of `P150` found the original A-vs-B framing was not
exhaustive: a third reading (C, virial/self-gravitating internal kinetic
energy — the kinetic energy of a body's own sub-parts insofar as it
supports the body against its own self-gravity) is at least as
physically motivated as A or B for a *gravity* theory, and independently
gives `k≈0` for solid laboratory test masses (a 100g test mass is
EM-bound, not self-gravitating) — the same conclusion as Reading A, via
a structurally different, arguably more principled route. **Two of
three defensible readings now converge on `MAPPING-UNAVAILABLE` for
solids; only Reading B allows `P151` to proceed at all.**

Also fixed: (1) a "13 orders of magnitude" figure conflated the `k/M`
ratio (~10.5 orders, independently reverified) with `docs/118`'s own
separate `F_d/F_m` dipole-correction-ratio figure; (2) `docs/118`'s
corona choice was characterized uncharitably (it states an explicit ICM-
analogy principle, not an unmotivated number-shopping choice — the weak
point is the analogy, not the arithmetic); (3) Reading B was never
applied *consistently* to the Sun in the original draft — doing so gives
`k_Sun/M_☉≈GM_☉/(R_☉c²)≈2.1×10⁻⁶` (independently computed here), on the
SAME order as clusters, not 14 orders of magnitude safer as `docs/118`
concluded using its corona-only value — meaning `docs/118`'s own
Cassini-safety conclusion does not survive under Reading B and needs
independent re-checking in `P151`, not reuse.

**Verdict corrected: `MAPPING-DEFINITION-DEPENDENT, LEANING UNAVAILABLE`**
(from the original `MAPPING-AMBIGUOUS`, which implied a misleading
symmetric split). `P151`, if it proceeds, must (a) carry Reading B's
label explicitly as "the one of three readings that allows any signal
at all," not "the more defensible option," and (b) re-check the
solar-system-scale (Cassini-type) constraint under Reading B, since
`docs/118`'s own prior safety margin does not carry over.
