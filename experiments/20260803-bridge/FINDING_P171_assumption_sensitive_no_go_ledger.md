# FINDING P171 — assumption-sensitive no-go ledger for the repulsive
# dipole (formalizing and extending `FINDING_P162`)

**Date:** 2026-08-31
**Labels:** NOT_VALIDATION · NOT_REFUTATION · OUR_RECONSTRUCTION ·
NO_AUTHOR_ERROR · L0 descriptive (classifying which theory classes are
logically compatible with a target structural feature — not a causal
claim about the real universe, not a claim about which class is true)
**Source:** cross-domain methodological transfer, proposed by the user
(von Neumann no-go-theorem methodology: a no-go is only as strong as
its explicit assumption list; the useful next question is never "is
the theorem true" but "which minimal assumption, relaxed, opens the
result"). Evaluated and scoped 2026-08-31.
**Status tags (per `docs/151_status_separation_rule.md`, dogfooded
here):**
> **Empirical/Model status:** N/A — this file is a logical/literature
> classification exercise, not a fit or numerical claim.
> **Ontological/mechanistic interpretation status:** SHARPENED, NOT
> RESOLVED, WITHIN THIS PROJECT'S OWN ENUMERATED 8-PREMISE SET (which
> `docs/131` itself already partially named, §1/§3 below) — narrows
> "no mechanism found" (`FINDING_P162`) to a specific, smaller set of
> unexamined premises. Not a claim about the space of all possible
> covariant completions (see §4 item 4).
> **Causal/cosmological claim status:** N/A — no claim about what the
> real universe does.
> **Correction (2026-08-31, context-asymmetric skeptic-caught, five
> points, independently re-verified before applying):** the first draft
> (a) conflated "used in the no-go's stated hypothesis set" with the
> stronger, von-Neumann-critique sense of "necessary" (dropping ONLY
> this premise, holding the rest fixed, provably opens a channel) — the
> ledger's own "Checked?" column empirically disconfirms the stronger
> reading for several rows it had marked YES. Column renamed and split
> (§2). (b) Row 3 (EP-respecting) claimed "no source found describing a
> SIGN flip" for screening mechanisms — **independently re-searched**:
> symmetron theories DO have a real sign structure (domains of opposite-
> sign VEV, `φ₊≠−φ₋`, separated by domain walls under explicit symmetry
> breaking) that the first draft missed — demoted to CHECKED-NARROW, not
> CHECKED-NEGATIVE. (c) **Row 5 (static/quasistatic limit) was
> substantively wrong, not just overclaimed** — `docs/131`'s own
> original text (re-read directly, not only via `FINDING_P162`'s quote
> chain) already names "a driven non-equilibrium theory (not a static
> force law)" as an explicitly UNRESOLVED residual branch. The first
> draft's WebSearch checked only a narrower sub-case (retardation/
> memory effects, antimatter repulsion) and incorrectly marked the
> whole premise CHECKED AND NEGATIVE — corrected to GENUINELY OPEN,
> matching `docs/131`'s own assessment, which this file should have
> deferred to rather than substituting a narrower search. (d) The
> "rows 1+2 collapse to one root cause" claim was asserted, not argued
> — softened; `docs/131`'s own text frames the ghost-free-vector
> question as ONE combined residual branch ("not known to exist"),
> which this file now cites directly instead of re-deriving. (e) §3's
> synthesis swept Row 4's own INCONCLUSIVE status under "every...
> fails" and phrased its conclusion as an existence claim about the
> full space of completions, contradicting §4 item 4's own disclaimer
> — reworded throughout.

**Continues/answers:** `FINDING_P162`'s own closing state ("no mechanism
has been found... standard or exotic") — this file does not find a new
mechanism either, but converts the search from an open-ended "which
mechanisms have we tried" into a closed, auditable "which assumption in
the no-go's own premise set remains genuinely unexamined."

## 0. Premise — `NO_AUTHOR_ERROR`

Every row below bounds only this project's own attempted covariant
completions (`docs/130`, `docs/131`, `FINDING_P162`) and this session's
own literature check — never a claim about v82's own theory. A "GENUINELY
OPEN" verdict on a row means "this project has not tested or found a
tested example," not "no such theory can exist."

## 1. The axiom set — extracted, not invented, and docs/131's own three
named residual branches (re-read directly, not only via `FINDING_P162`'s
quote chain)

`docs/131`'s own conclusion sentence already names most of these
premises explicitly: "the repulsive dipole is **not** the static
weak-field limit of any **equivalence-principle-respecting**,
**positive-energy**, **ghost-free**, **local** realization in which
`k_A` gravitates as the energy it is defined to be." `docs/130` adds one
more, implicit in its own method ("stable **scalar**–scalar exchange is
attractive... repulsion requires **vector** mediation"): the
mediator/field-content choice. `FINDING_P162` §4 tests one further
premise implicitly (linear/classical response, vs. one-loop quantum
correlation effects) and one more (single effective point-particle per
node, vs. many-body/statistical averaging).

**Crucially, `docs/131` (`docs/131_L1_weakfield_matching_result.md:95-99`)
already names three residual open branches itself, verbatim, each
requiring abandoning a named standard assumption — this file's job is
to check each against this project's cumulative work, not to invent
them:**

> "Residual open branches (honestly not ruled out, but each requires
> abandoning a standard assumption): a `k_A` that is somehow NOT its own
> mass-energy (contradicting the preprint's definition); a genuinely
> exotic ghost-free vector/antisymmetric sector engineered to give
> static `1/r³` repulsion (**not known to exist**); a driven
> non-equilibrium theory (not a static force law). None is in the
> corpus."

These three map directly onto rows 1 (`k_A` redefinition), 2+7 combined
(ghost-free vector/antisymmetric sector), and 5 (driven non-equilibrium)
below — **`docs/131` itself, in 2026-07-22, already assessed all three
as unresolved.** This file's contribution is checking whether any of
this project's SUBSEQUENT work (`FINDING_P162`, and this session's own
literature search) has since closed any of them — and being honest when
it has not.

**Eight named premises**, none invented for this file:

1. Positive energy (`k_A` gravitates as the ordinary positive
   kinetic/thermal energy it is defined to be)
2. Ghost-free (no higher-derivative / Ostrogradski pathology)
3. Equivalence-principle-respecting / universal metric coupling
4. Locality
5. Static/quasistatic limit
6. Linear (classical, tree-level) response
7. Scalar mediator (the specific field content `docs/130` tested first)
8. Single effective point-particle per node (not many-body)

## 2. The ledger

**Column definitions (corrected — skeptic-caught terminological slip):**
"Used in stated hypothesis set?" means the premise appears in the
sentence(s) `docs/130`/`docs/131` use to state their own conclusion —
a weak, factual, always-checkable fact. "Dropping ALONE opens a
channel?" is the strong, von-Neumann-critique sense of "necessary" —
an EMPIRICAL result from actually checking, not inferred from the
premise's presence in the stated hypothesis. These are different
questions and the table keeps them in separate columns on purpose.

| # | Assumption | Used in stated hypothesis set? | Dropping ALONE opens a channel? | If relaxed → theory class | Checked? (this project) | Verdict |
|---|---|---|---|---|---|---|
| 1 | Positive energy (`k_A` as its own mass-energy) | YES — `docs/131` explicit, AND `docs/131`'s own residual-branch #1 | **UNTESTED** | Redefined/exotic `k_A` not equal to ordinary mass-energy | NOT tested by this project or `docs/131` — `docs/131` itself calls this "contradicting the preprint's definition," i.e. not a modeling choice TJB has made | **GENUINELY OPEN** (per `docs/131`'s own 2026-07-22 assessment, unchanged) |
| 2+7 | Ghost-free AND mediator field content (combined — `docs/131`'s own residual branch #2 treats these together) | YES — `docs/130` tested scalar (attractive), `docs/131` tested vector-WITH-ghost-freedom-and-positive-energy held fixed (still attractive) | **PARTIALLY TESTED, still open in combination** — vector mediation ALONE (holding ghost-freedom AND positive-energy fixed) does NOT open a channel (`docs/131`'s own `CANDIDATE-L1` result); a ghost-free vector/antisymmetric sector that ALSO relaxes something else is `docs/131`'s own words "not known to exist" | Ghost-free vector/antisymmetric sector engineered for static 1/r³ repulsion | `docs/131` itself already assessed this combined branch as unresolved; this session found no literature closing it either direction | **GENUINELY OPEN** (per `docs/131`'s own assessment; this file did not find grounds to close it, and softens the first draft's unargued "rows 1+2 collapse to one root cause" claim — they may be related, not proven equivalent) |
| 3 | EP-respecting / universal coupling | YES — `docs/131` explicit | **NARROW-NO** — the one relaxation branch checked (chameleon/symmetron/Vainshtein density-dependent screening) does not open a channel by itself | Screened modified gravity | **CHECKED-NARROW** (this session): screening's dominant, best-known effect is coupling-MAGNITUDE suppression in high density — confirmed. Independently re-checked after skeptic review: symmetron theories under EXPLICIT symmetry breaking do have domains of opposite-sign VEV (`φ₊≠−φ₋`) separated by domain walls — a genuine sign-related structure this file's first draft missed — but no source found connecting this to a two-body inter-cluster force sign flip specifically. Other EP-relaxation branches (species-dependent Yukawa couplings, dilaton/Damour-Polyakov tensor-scalar) were NOT searched this session. | **CHECKED-NARROW** — one specific branch (density-magnitude screening) does not open a channel; the broader EP-relaxation class is not exhausted |
| 4 | Locality | Implicit background assumption, not explicitly named in `docs/131`'s own sentence | **INCONCLUSIVE** | Nonlocal gravity (IR-modified propagator/kernel) | **CHECKED, narrow** (this session, 2 searches): field's own conventional statement — "not possible to generate repulsive gravity from any positive mass"; nonlocal massive gravity (PRL 132,191402) and Deser-Woodard-class literature searched, neither found addressing this specific two-body sign question either way | **CHECKED, INCONCLUSIVE** — genuinely underdetermined by this session's search, not exhaustive |
| 5 | Static/quasistatic limit | YES — `docs/131` explicit ("the static weak-field limit"), AND `docs/131`'s own residual-branch #3 ("a driven non-equilibrium theory") | **UNTESTED as `docs/131` itself states it** | Driven/non-equilibrium dynamics (broader than mere retardation) | **CORRECTED (skeptic-caught) — this session's search checked only a NARROW sub-case** (retardation/GW-memory-effects, and a gravitationally-repulsive-antimatter literature independently fetched and confirmed inapplicable to ordinary composite matter) — neither addresses the FULL "driven non-equilibrium" class `docs/131` itself names (actively pumped/dissipative systems, non-adiabatic node dynamics). The first draft incorrectly marked this CHECKED-AND-NEGATIVE; corrected to defer to `docs/131`'s own original, still-accurate assessment. | **GENUINELY OPEN** (per `docs/131`'s own 2026-07-22 assessment — this session's narrower search did not close it, contrary to the first draft's claim) |
| 6 | Linear (classical) response | Implicit — `docs/130`/`docs/131` work in classical field theory | **NO** | One-loop quantum-gravity correlation/fluctuation forces (Casimir-Polder-type) | **CHECKED** — `FINDING_P162` §4 (prior session): real literature exists (Ford-Hertzberg-Karouby, PRL 116, 151301, 2016), attractive for similar-composition bodies, the one known repulsive channel explicitly subdominant for identical polarizabilities, and quantum-loop-suppressed (astronomically negligible at cluster scales) | **CHECKED AND NEGATIVE** — wrong-signed AND too small; the one row where "dropping alone" was genuinely tested and genuinely fails |
| 8 | Single point-particle per node | Implicit in `docs/130`/`docs/131`'s own constructions | **NO** | Many-body / statistical (mean-field) node ontology | **CHECKED** — `FINDING_P162` §4: mean-field averaging over many EP-respecting particles reduces to the identical `ρ+3P/c²` mechanism already tested, not a new escape route | **CHECKED AND NEGATIVE** |

**Row 9 — added 2026-09-07 by `FINDING_P204`, the ledger was not
complete.** This row was absent from the 8 above and turned out to be
the one the whole `CANDIDATE-L1` question hinges on. Verified absent
before adding: `grep -i "inter-node|between the nodes|vacuum|empty"`
over this file returned a single hit, in an unrelated Ostrogradski
context.

| # | Assumption | Used in stated hypothesis set? | Dropping ALONE opens a channel? | If relaxed → theory class | Checked? | Verdict |
|---|---|---|---|---|---|---|
| 9 | **The node's dipole is a BODY-level moment attached to a worldline** (so Racine–Flanagan Eqs. 71/82 apply and it is pure gauge) | Implicit and unexamined in `docs/130`/`docs/131` — their "polarizable **point particle**" presupposes it | **YES** — this is the one assumption whose relaxation demonstrably opens the channel | Fluid-level **polarization field** with no worldline (Blanchet–Le Tiec's actual object): the mass-dipole gauge argument does not apply to a dipole-per-unit-volume | **CHECKED, `FINDING_P204`** — `[VERIFIED-arXiv 0804.3518]` B-LT's dipole is a medium polarization, not a body moment; `docs/131` compressed it to a point particle and thereby destroyed the property that evaded R&F | **OPEN AND LOAD-BEARING** — the only row so far whose relaxation is both untested here and known to work in a real published theory |

**Consequence for §3's synthesis below:** it was written against an
8-row ledger believed complete. Row 9 does not overturn `docs/131`'s
FAIL, but it does mean the no-go was never tested against the one
formulation in which the parent theory actually lives.

## 3. Synthesis (corrected — skeptic-caught overclaim)

**This does not sharpen `FINDING_P162` as much as the first draft
claimed.** Re-reading `docs/131` directly shows it already, in
2026-07-22, correctly identified exactly three residual open
branches (§1 above) — this file's actual contribution is narrower than
"discovering which premise is unexamined": it is (a) organizing those
three branches plus `docs/130`/`FINDING_P162`'s own additional checked
premises into one auditable table, (b) confirming two of `docs/131`'s
three branches remain open after this session's own literature search
(rows 1 and 2+7), and (c) **catching that this file's own first draft
had incorrectly claimed to close the third branch (row 5, driven
non-equilibrium) when it had only checked a narrower sub-case**
(retardation) than what `docs/131` actually left open.

**Honest current state:** of 7 table rows (8 premises, rows 2+7
merged), 2 are cleanly CHECKED-AND-NEGATIVE
(rows 6, 8 — genuinely tested, genuinely fail), 2 are CHECKED-NARROW
(rows 3, 4 — one branch tested, broader class not exhausted), and
**3 remain GENUINELY OPEN, matching `docs/131`'s own original
assessment** (rows 1, 2+7, 5) — unchanged by this project's subsequent
work, including this session's own search. This is a real, useful
result — it confirms `docs/131`'s own 2026-07-22 residual-branch list
is still accurate more than a month later, across `FINDING_P162` and
this session's own fresh literature check — but it is a **confirmation
of an existing assessment**, not primarily a new sharpening. The one
genuinely new content this file adds: `docs/131`'s branch #3 ("driven
non-equilibrium") is the LEAST literature-explored of the three (a
single narrow search, easily distinguished as a direction for future
work), while branches #1 and #2 have had no dedicated search attempt
at all in this project's history. If a next step is chosen from this
ledger, branch #1 (`k_A` redefinition) or #3 (driven non-equilibrium)
are the least-explored, and therefore the highest-information-gain
next literature checks — not branch #2 (ghost-free vector sector),
which `docs/131` itself already flagged as "not known to exist" after
its own dedicated attempt.

## What this file does NOT establish

1. **Not a claim that a ghost-like, redefined-`k_A`, or driven-non-
   equilibrium completion exists, is viable, or is what TJB intends**
   (`NO_AUTHOR_ERROR`) — only that these are the three premise-
   relaxations `docs/131` itself already named and this project has not
   since closed, within this file's own enumerated 8-premise set (item 4
   below) — not a claim about the space of all possible completions.
2. **Does not build or test any of the three open branches** (`k_A`
   redefinition, ghost-free vector/antisymmetric sector, driven
   non-equilibrium dynamics) — they remain open precisely because that
   work has not been done, by `docs/131`, `FINDING_P162`, or this file.
3. **Row 4 (locality) and row 3 (EP-relaxation) are checked-narrow, not
   resolved** — a more targeted literature search, or an explicit
   calculation, could move either in either direction; this file's own
   two-query search per row was not exhaustive.
4. **Does not claim the eight-premise (seven-row, after 2+7 merge) list
   is complete** — it is extracted from what `docs/130`/`docs/131`/
   `FINDING_P162` actually used, not derived from a first-principles
   enumeration of every possible GR-modification axis (e.g.
   dimensionality, signature convention, and several others are not
   addressed here).
5. **Ghost/negative-energy completions are generically pathological**
   (Ostrogradski instability, vacuum decay) in the broader literature —
   this file does not independently verify that general fact for this
   specific case, only notes it as the standard reason this avenue is
   usually disfavored, which is itself part of why it remained
   unexplored rather than a reason to treat it as resolved.
6. **This file's own first draft was itself a live example of the
   von-Neumann-critique failure mode it was modeled on** (asserting
   "necessary" without empirically testing the strong sense, and
   claiming a branch closed on the strength of a narrower check than
   `docs/131`'s own original scope) — corrected above, kept as an
   explicit note because the same failure mode is exactly what this
   ledger format exists to guard against, and it is worth being honest
   that guarding against a failure mode does not make a file immune to
   it on the first attempt.

NOT_VALIDATION * NOT_REFUTATION * OUR_RECONSTRUCTION * NO_AUTHOR_ERROR
