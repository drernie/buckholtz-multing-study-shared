# FINDING E4 — Complete Class I/II/III map of v82's own inputs, and:
# TJB's own THIRD named limitation (β₁/β₂ near-cancellation) is not new
# ground — this project already has a substantial, relevant body of
# work on it

**Date:** 2026-09-06
**Continues:** `FINDING_E1`-`FINDING_E3`. Fourth and fifth steps of the
5-step autonomous follow-up, consolidated into one synthesis rather
than run as two separate full passes, since the remaining ground
(claim-decomposer mapping, negative-space check) is best served by
tying `E1`-`E3` together with this project's own prior work rather than
opening further new sub-investigations.

## Complete Class I/II/III map (claim-decomposer pass on v82's own
## construction)

TJB's own text classifies most, but not all, of MULTING's inputs
explicitly. Completed here, reading v82's own text directly (not
inferred):

| Input | v82's own classification | Where stated |
|---|---|---|
| Cosmic Chronometer `H(z)` (31-pt compilation) | **Class I**, data-proximate | `v82.md:564-566` |
| Gas mass, thermal energy (ICM) | **Class I** (`"grounded in real X-ray data"`) | `v82.md:327` |
| Temperature (`T_keV`, self-similar step) | Class II by default, upgradeable to Class I `"if grounded in directly measured cluster X-ray scaling relations"` | `v82.md:566-569` |
| Mass evolution `m_X(z)` (Eq. 10) | **Class II**, retained-theoretical | `v82.md:586-587` |
| Inter-node separation law (Sec. II.C) | **Class II** — `"the least FLRW-flavored input... even though it remains a theoretical assumption"` | `v82.md:587-589` |
| Node radius `r_X(z)` via `ρ_crit(z)` (Sec. II.F) | **Class III**, circular — `"the most serious residual dependence"` | `v82.md:601-613`, `FINDING_P199` |
| `H₀,anchor = ṡ₀/s₀` (Sec. IV.M) | Not given the "Class III" label explicitly, but TJB states it is `"circular in the same sense already flagged for r_X(z)"` | `v82.md:1306-1307`, `FINDING_E2` |

**One genuine gap closed by this pass:** `H₀,anchor` is structurally a
second Class III item — TJB's own text draws the equivalence directly
but never assigns the formal label in Section IV.M itself (the label
is only used in Section II.F). This is a labeling completeness note,
not a substantive disagreement with TJB's own analysis.

## TJB's own third named limitation is NOT new ground for this project

Section IV.O / the Conclusion (v82 p.33, quoted in `FINDING_E2`) names
**three** limitations as "evidence of how much work this direction
still requires": (1) the near mutual-cancellation of `β₁`/`β₂` force
terms (Table III), (2) the `r_X(z)` Class III circularity, (3) the
abandoned `H₀,anchor` first-principles attempt.

This project has already done substantial, real work on **item (1)**,
entirely independent of the "evidence vs. authority" thread — under a
different name, "absolute scale / observable mapping" (one of the 4
named bottlenecks in `docs/147`):

- `P133`: formal identifiability audit — `(A,g,κ)` not jointly
  identifiable from `(A·g², A·k², k/g)` alone, `rank(Jacobian)=2<3`,
  two independent symbolic methods, positive control.
- `P176`/`P190`: the `β₁`/`β₂` degeneracy is confirmed **approximate**,
  not exact — the level-set slope `-E1(z)/E2(z)` varies `12.7%` across
  the real `z=0.07-2.33` data range, ruling out the strongest "no data
  could ever break this" reading, while confirming it is real and
  severe over the fitted range.
- `P191`-`P194`: Fisher-information forecasts — synthetic `z`-points in
  a specific window (`z∈{12,14,16}`) give up to `5.4×` more shrinkage
  of the degeneracy than the originally-tried window, with a real,
  grid-converged domain-of-validity boundary found at `z≈16.957`
  (`H²(z)<0` beyond it for TJB's own spotlighted fit).

**This means: of TJB's own three named limitations, this project
already has a real, tool-verified, multi-step body of work on one
(`β₁`/`β₂`), is actively deepening a second (`r_X(z)`, `P196`-`P202` +
this thread's `E1`-`E2`), and has just opened a third (`H₀,anchor`,
`E2`-`E3`).** None of TJB's own named "how much work this direction
still requires" items is untouched ground for this project.

## Synthesis — where the "de-conflating evidence from authority" thread
## actually stands

1. **The topic itself is not open ground** (`E1`) — a mature,
   decades-deep literature (cosmography, FLRW/Copernican-principle
   consistency tests, distance-duality-relation tests) already does
   versions of exactly what TJB describes, giving real vocabulary and
   tools to connect to rather than reinvent.
2. **TJB's own paper already contains two explicit, self-diagnosed
   instances of the pattern** (`r_X(z)`, `H₀,anchor`) — both genuinely
   found by TJB himself, both correctly analyzed, and TJB's own text
   already draws the connection between them.
3. **A close, literature-grounded look at whether `H₀,anchor`'s own
   circularity is truly unfixable found one genuine, scale-matched
   structural exception (kSZ's velocity channel)** — real but
   unproven as a working fix, correctly narrowed after a skeptic pass
   caught an overclaim (`E3`).
4. **All three of TJB's own named "how much work this requires" items
   map onto work this project has already done or is actively doing**
   — the "de-conflating evidence from authority" topic he proposed as
   possibly independent of his own work turns out to be the SAME
   underlying set of open problems this project's own bottleneck lines
   (`docs/147`) were already organized around, just newly connected and
   named under his own framing.

## What this does NOT establish

1. Does not resolve any of the three named limitations — `β₁`/`β₂`
   remains a real, severe (if not exact) degeneracy over the fitted
   range; `r_X(z)`'s circularity remains unresolved (`P196`-`P202`'s
   own residual investigation concluded `PART-OF-MACROSYSTEM`, not
   fixed); `H₀,anchor`'s circularity remains real, with kSZ only a
   flagged, unproven, structurally-partial exception.
2. Does not draft or send anything to TJB. Any future correspondence on
   this thread is a separate, explicit decision.
3. `NO_AUTHOR_ERROR` — entirely a mapping of this project's own
   existing and new work against TJB's own already-published
   self-assessment, not a claim about v82's correctness.
